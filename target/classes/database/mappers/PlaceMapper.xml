<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.food.sbproject1.place.PlaceMapper">
	
	<!-- 상세 글 조회 -->
	<select id="getOne" resultType="PlaceVO" parameterType="PlaceVO">
		select * from place where num = #{num}
	</select>
	
	<!-- 게시글 수 조회 -->
	<select id="getCount" resultType="java.lang.Long">
		select count(num) from place
	</select>
	
	<!-- 맛집 리스트 -->
	<select id="getList" resultType="PlaceVO">
		select * from place
	</select>
	
	<!-- 맛집 추천 작성 -->
	<insert id="setInsert" parameterType="PlaceVO">
		insert into place(title, writer, contents, regDate, hit, menu, restaurant, address, phone, openTime, closeTime, price, rest)
		values(#{title}, #{writer}, #{contents}, sysdate(), 0, #{menu}, #{restaurant}, #{address}, #{phone}, #{openTime}, #{closeTime}, #{price}, #{rest})
	</insert>
	
</mapper>  